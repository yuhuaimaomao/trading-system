# 更新记录

重大变更、bug 修复、架构调整的时间线记录。

---

## 2026-06-10 — 盯盘管线全面优化 + 五个新因子

### 收盘审计驱动修复（21:00）

基于当日 AI 审计发现的 4 个问题：

**1. entry_rule="standard" 绕过 zone_pos 过滤**：修复 `buy_decision.py`，standard 入场规则新增 zone_pos>0.5→暂缓。当 market pattern 从 normal 变成 unknown 时，entry_rule 从 confirm 变成 standard，原代码 standard 不限制 zone_pos，导致被 confirm 过滤的股票在 pattern 切换后直接通过。当日 6 只 14:33 被过滤的股票在 14:53 全部绕过过滤开仓。

**2. position_mult=0 硬阻断**：`buy_decision.py` 新增：position_mult≤0 时直接跳过，不计算仓位、不发买入信号。原逻辑仅在 RiskEngine 中做软性限制，sizing 路径可能绕过。

**3. zone_pos 动态阈值**：`confirm` 入场规则不再固定 50% 阈值。极端普跌日（跌涨比>3:1）下板块走强→75%，板块一般→60%。逻辑：普跌日能维持 zone_pos>50% 的股票具有稀缺的相对强度，不应强制等回调。

**4. regime_unstable_day 机制**：`MarketRegime` 新增 `regime_unstable_day` 字段。`_check_regime_jitter()` 追踪窗口内切换，≥4 次且全部 low confidence → 标记。标记后 entry_rule 强制 next_day，position_mult 封顶 0.3。当日 regime 切换 25+ 次全 low confidence 但系统照常交易。

### AI 基础设施调整

**统一落盘**：`system/ai/ai_service.py:_call()` 统一处理所有 AI 调用的 prompt/response 落盘。prompt → `storage/prompts/{task}_prompt_{date}.txt`，response → `storage/reports/{task}_ai_response_{date}_{model}.txt`。删除 `strategy/ai_advisor.py` 和 `review/review_analyzer.py` 中各自落盘代码。

**max_tokens 移除**：所有 AI 调用不再限制 max_tokens，由 API 自行决定。涉及 6 个文件。

**模型配置简化**：盯盘子业务（chase/swap/index/trapped/breakout）统一合并为 `AI_MODEL_WATCHER`。`AI_MODEL_SCREENING`/`AI_MODEL_AUDIT` 不设则回退到 `AI_MODEL=deepseek-v4-pro`。

### 架构修复

**QMT 行情数据流切换**：`_get_realtime_prices` 从 HTTP 直连 QMT 改为优先使用 Collector TCP 推送的 `_market_snapshot`。Collector 每 7 秒推送全市场 4717 只股票快照到 Watcher 内存，不再每轮扫描走 HTTP（READ_TIMEOUT=120s，曾导致 Watcher 卡死）。

**健康检查修复**：`_cross_validate_index_high_low` 中 `index_close_low` 初始化前跳过校验，避免用 K 线高低点数据与收盘价序列对比产生误报。

### 引擎1 买入过滤放宽

`evaluate_buy()` 十维过滤中大部分拒绝阈值降级为警告+减仓：
- 板块拒绝 -1%→-2%，数据不足从不买到减仓
- 布林%B 拒绝 90→95/98，日内RSI 85→92，MACD bar -0.5→-0.8，KDJ J 100→110
- 买入区 zone_pos 拒绝 0.85→0.95，涨停距离 2%→1%，主力流出 -5%→-8%
- 止跌判断 reversing 从 >1.005x 降到 >0.998x
- 盘前信号跳过 entry_rule 二次拦截
- 复盘/板块候选 AI 异步结果放开推送

**市场宽度分级**：`_check_buy_candidates` 中宽度过滤仅对盘前信号/复盘推荐生效（`source in ("signal","review")`），盘中动态发现和回踩扫描不拦。

### 引擎2 策略转型

从「底部发力」（精确抄底）转为「趋势确认追入」：
- 运行时段 10:00-11:30 → **9:30-14:30**
- 筛选条件 四条件 AND → 两核心（涨幅+板块走强）+ 加分项（量能/加速/板块排名）
- AI 不出售买入：高分(≥70)直接买，中分(50-69)AI辅助，低分仅提醒
- 仓位从固定 5000 改为引擎1统一计算函数
- 板块排名从硬过滤改为加权加分

### 新增五个因子

| 因子 | 实现位置 |
|------|---------|
| 资金流连续趋势 | `StockReader.get_money_flow_trend` → `evaluate_buy` |
| 波动率异动 | `StockReader.get_volatility_breakout` → `evaluate_buy` |
| 板块连续性 | `_scout_layer2_rank` 中使用 `_sector_trend_continuity` |
| 盘口变化率 | `_get_order_book_imbalance` 返回 delta → `evaluate_buy` |
| 开盘强度分析 | `_get_opening_strength` → `evaluate_buy` |

### 关联文件

- `trade/core/watcher.py`: _get_realtime_prices 改用 snapshot, _alert 板块分方向
- `trade/decision/buy.py`: evaluate_buy 门槛放宽 + 五个新因子接入
- `trade/decision/buy_decision.py`: entry_rule 跳过, BuyEvalInput 扩展, 宽度分级
- `trade/detect/intraday_scout.py`: 策略转型, 时段扩展, 板块连续性
- `trade/core/health_checks.py`: index_close 初始化防护
- `data/market/stock_basic.py`: get_money_flow_trend, get_volatility_breakout
- `data/repo/__init__.py`: 新增代理方法

---

## 2026-06-08 — 数据层按业务线重构

**变更**：`data/` 目录从「按操作类型」（repo/readers）重构为「按业务线」组织。

**新结构**：
```
data/
├── _base.py          # 共享基类 + connect() + get_db_conn()
├── schema.py         # DDL + 迁移
├── market/           # 行情基础数据（跨域共享）
├── trade/            # 交易线
├── strategy/         # 策略线
├── review/           # 复盘线
├── audit/            # 审计线
├── collect/          # 采集（不动）
├── process/          # 加工（不动）
├── repo/             # [兼容层]
└── readers/          # [兼容层]
```

**关联改动**：
- 业务代码 22 个文件消除 `import sqlite3`，统一通过 `data._base.connect()` 获取连接
- 内联 SQL 约 120 条提取到对应 data/ 模块
- 新增 data 模块：`market/stock_basic.py`, `market/sector_data.py`, `market/events_data.py`, `trade/signals.py`, `trade/orders.py`, `trade/portfolio.py`, `strategy/funnel.py`, `strategy/morning.py`, `strategy/screening.py`, `review/tracker.py`, `review/predictions.py`, `review/analysis.py`, `audit/decision_log.py`
- `data/repo/` 和 `data/readers/` 保留为薄包装兼容层

---

## 2026-06-08 — stock_basic schema 对齐

**变更**：schema.py 中 `stock_basic` CREATE TABLE 与生产库对齐，共 50 字段。
**规则**：新增字段无需手动 ALTER TABLE（SQLite 特性），但 schema.py 和测试 DDL 子集需同步更新。

---

## 2026-06-07 — 趋势筛选位置感知

**新增因子**：`check_price_position` 按 20 日高低区间分「高位运行」/「低位启动」。
**新增因子**：`check_vol_price_rise`（量价齐升）和 `check_sector_vol_price`（板块量价齐升）。
**盘中预趋势**：`IntradayScoutMixin._scout_check_pretrend` 四形态 + 六维打分。

---

## 2026-06-06 — trade_signals.account 列修复

**问题**：CREATE TABLE 中 UNIQUE(trade_date, stock_code, account) 引用了不存在的 account 列。
**修复**：CREATE TABLE 直接包含 `account TEXT DEFAULT 'real'`。

---

## 历史约定修正

| 日期 | 内容 |
|------|------|
| 2026-06-07 | 回踩买入区公式修正（原来 `*0.5` bug 导致买入区倒挂） |
| 2026-06-06 | 缺 sl/tp 信号自动从支撑阻力位补算，不再死循环 |
| 2026-06-06 | trade_signals.account 列缺失修复 |
